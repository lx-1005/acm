

对于给定数组a[],
1. O(n)预处理前缀和, 可实现O(1)查询任意子区间a[l~r]的和
2. O(nlogn)预处理所有长度为2的幂的子数组的最值, 可实现O(1)查询任意子区间a[l~r]的最值
    1. 预处理所有长为1, 2, 4, 8, ...的子数组的最值
    2. 任意区间a[l~r]都可以用两个长为k的子区覆盖, k=不超过(r-k+1)/2的最大整数, 原理是求最值可以有冗余
        min(al, ..., ar) = min(
                                min(a[l], a[l+1], ..., a[l+k-1]),
                                min(a[r-k+1], ..., r[r-1], a[r])
                                )